name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  # ── TypeScript type-check + build ──────────────────────────────────────────
  typescript:
    name: TypeScript (SDK + Indexer + App)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Build SDK
        run: npm run build --workspace=sdk

      - name: Type-check Indexer
        run: npx tsc --noEmit
        working-directory: indexer

      - name: Type-check Scripts
        run: npx tsc --noEmit
        working-directory: scripts

      - name: Build App (Next.js)
        run: npm run build --workspace=app

  # ── Rust contracts ─────────────────────────────────────────────────────────
  contracts:
    name: Soroban Contracts (Rust)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            contracts/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('contracts/Cargo.toml') }}
          restore-keys: ${{ runner.os }}-cargo-

      # The testutils feature pulls in soroban-env-host which has a known
      # ed25519-dalek v3 incompatibility. We build release WASM (no testutils)
      # for CI. Run tests locally with `cargo test` after `rustup target add wasm32-unknown-unknown`.
      - name: Check contracts compile (no testutils)
        run: cargo check --release --target wasm32-unknown-unknown --workspace
        working-directory: contracts

      - name: Build contracts (release WASM)
        run: cargo build --release --target wasm32-unknown-unknown --workspace
        working-directory: contracts

  # ── Lint ───────────────────────────────────────────────────────────────────
  lint:
    name: Lint (Next.js)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm install

      - name: Run ESLint
        run: npm run lint --workspace=app
